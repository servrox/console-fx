# ConsoleFX review and fixes — 15 September 2026

**Four confirmed issues are fixed. Final Standards and Spec reviews have no remaining actionable findings. Local verification is complete.**

- Baseline: main `39f3b5f15bc501f9cd35622ad111efb4454c4c89`.
- Scope: the current core, adapter, studio, accepted specs and release tooling.
- Worktree: `fix/console-fx-review-20260915`; existing worktrees and indexes are preserved.
- Method: independent Standards and Spec reviews, bounded reproductions, individual fixes, then re-review and milestone validation.

| Review axis         | Finding                                                                                      | Correction                                                                                                                           | Passing evidence                                                                                       |
| ------------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| Standards           | S-01: wrapping can split a grapheme at a styled-run boundary                                 | `fit/v2` checks the complete paragraph before accepting a wrap; preserves text and explicit fallback                                 | Core Unicode/fitting contracts, legacy compatibility fixtures and native output observation            |
| Standards           | S-02: adding Cinematic Metal to an existing motion effect creates an unsupported combination | The shared capability resolver supplies effect additions; the editor disables and explains incompatible choices and rechecks actions | Generalized effect-combination matrix and the existing cinematic browser journey                       |
| Spec                | P-01: canceling renderer, preset or shared-scene confirmation loses focus                    | Native modal dialogs own interaction blocking; readiness alone disables the editor fieldset                                          | One browser recovery journey covering the three distinct triggers, unchanged recipe and silent actions |
| Parent source audit | D-01: public Docs still describes `next` as 0.1.0                                            | Remove duplicate hardcoded release versions; retain the documented installation channel                                              | Source diff, built page and public registry metadata                                                   |

The public registry was read on 15 September: both packages have `next: 0.2.0`
and `latest: 0.1.0`. Changes remain in the review worktree; this candidate has
not been committed, sent through GitHub CI, published or deployed. The core
correction has a [pending patch changeset](../../.changeset/preserve-styled-graphemes.md).
Testing tarballs retain the checkout's version and must not be republished as
0.2.0.

## Compatibility and ownership

- ADR-0018 expressly preserves `fit/v1`. Its historical cross-run segmentation remains unchanged; this correction applies to `fit/v2`.
- No profile artwork, saved scene/recipe envelope, motion support, dependency, renderer default or resource budget changes.
- The wrapping change uses the existing `Intl.Segmenter`; work remains bounded by validated text and candidate limits.
- An unreachable conditional in the sizing return was removed to keep the compiler within the existing bundle budget. The early return already covers calls with neither fitting nor sizing; returned metadata is unchanged.
- Governing decisions: ADR-0002, 0004, 0005, 0007, 0009, 0012, 0013, 0015, 0016 and 0018. No new architecture decision is needed.

## Final verification

Checks ran at the implementation milestone and finalization. No dependency or
test-runner configuration changed; the locked runner is Vitest 4.1.11.

| Layer                          | Result                                                                                                                   |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| Independent Standards review   | Both findings fixed; final re-review: 0 actionable findings                                                              |
| Independent Spec review        | Focus recovery fixed and reproduced in the browser; final re-review: 0 actionable findings                               |
| Source and build               | `build:vercel`, `format:check`, `lint`, `typecheck` and `git diff --check` pass                                          |
| Vitest                         | 417 tests across 26 files pass, including the existing 93-case `fit/v1` compatibility matrix                             |
| Studio browser journeys        | 52 desktop/mobile checks pass                                                                                            |
| Package boundaries             | `build:packages`, `check:packages` and `check:bundle-size` pass                                                          |
| Isolated consumers             | JavaScript, TypeScript, React lifecycle, displayed recipes, Next production build/SSR and all 3 Next browser checks pass |
| Existing example compatibility | All 43 qualified defaults retain identical arguments, previews, layout reports, diagnostics and standalone code          |
| Native Windows DevTools        | All 6 new Unicode cases pass in each of Chrome and Edge; 12 observations total                                           |
| Concurrent-work preservation   | All 19 pre-existing worktree HEAD/index pairs and all 205 recorded files in the original checkout are unchanged          |

The compiler remains inside the existing gzip budgets: minimal CSS
9,065/10,240 bytes, complete CSS 25,594/25,600 bytes and SVG 25,593/25,600 bytes.
The complete compiler has very little remaining headroom; the budget was not
raised.

The native check used Windows 11 25H2 build 26220.9223, Chrome for Testing
153.0.8010.36 and Edge 153.0.4234.32. Both match the official stable feeds read
on 15 September: [Chrome](https://googlechromelabs.github.io/chrome-for-testing/last-known-good-versions-with-downloads.json)
and [Edge](https://edgeupdates.microsoft.com/api/products?view=enterprise).
Every case emitted once with the exact compiled arguments and preserved caption.
All eight native SVG crops and four text-fallback captures were visually
inspected. These observations cover the changed Unicode wrapping behavior;
the 43-example comparison is a separate byte-compatibility check.

An initial concurrent browser/consumer attempt was invalidated by both runners
using the same `test-results/` directory. Both complete suites then passed when
run sequentially. The initial native Chrome connection also timed out; resuming
the dedicated test target and dismissing Edge's test-profile setup dialog let
both native runs complete. These were harness/setup failures, not passing
application evidence.

Local fingerprints, log hashes and native capture references are recorded in
`.artifacts/review-20260915/receipt.json`. Raw diagnostics remain under
`.artifacts/review-20260915/`, `.artifacts/devtools/` and
`/tmp/console-fx-review-*`.

| Local testing artifact  | SHA-256                                                            |
| ----------------------- | ------------------------------------------------------------------ |
| Core tarball            | `d62c12f2e3833be9603f0442fee8e80fe594ead231d82804476f574ad1222ba6` |
| React tarball           | `8e513ab2d4d480a6d9edb0f80df598d7dd07e36e2ae2a01ee3c0ea43441fde10` |
| Native fixture compiler | `922a3429ef4cd1ad27aea87d20ffba7b7ee3f391cb310270ebd93e860684209b` |

Participant sessions, physical-device performance and real Safari observations
remain unobserved external checks. Screen-reader review remains nonblocking
under ADR-0013. These local results do not replace CI, publication or hosted
verification of this candidate.
