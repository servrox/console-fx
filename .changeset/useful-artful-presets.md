---
"@servrox/console-fx": minor
---

Add ten closed, static card presentations with typed supplied-content factories,
read-only slot/status metadata, strict compatibility diagnostics and complete
plain-text fallback. Add an explicit CSS compiler that allows SVG artwork to be
removed from simple CSS bundles while preserving the existing complete compiler.
