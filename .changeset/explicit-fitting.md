---
"@servrox/console-fx": minor
---

Add explicit SVG content fitting, independent output sizing, read-only geometry/confidence reports, optional local-font measurement batches and strict render-recipe parsing. Legacy calls preserve their output. Container sizing remains experimental; compact and studio integration are tracked separately.
